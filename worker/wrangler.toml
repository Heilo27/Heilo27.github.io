name = "survey-api"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Custom domain: survey-api.heiloprojects.com
# Set up via Cloudflare dashboard after first deploy

# Secrets (set via `wrangler secret put`):
#   GITHUB_TOKEN   — Fine-grained PAT (Contents:Write on survey-responses repo only)
#   GITHUB_OWNER   — "Heilo27"
#   GITHUB_REPO    — "survey-responses"
#   ALLOWED_ORIGIN — "https://heiloprojects.com"
